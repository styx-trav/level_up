#include "libby.h" //? maybe we'll need a different version of this for this one

int main(int argc, char **argv) //argv for the filename to open and fill
{
  //we set the "pixels"
  //size :: pixels that get drawn on screen
  //we then divide the img on screen into so many pixels, make an empty map of that ratio size
  //with a hook, we grab every left click, determine which "pixel" the mouse is in, add the color that pixel
  //right click for grabbing colors ? we could have a corridor on the left or right to grab them from
  //right click multiple time to get more of that color, divided in whatever makes sense for a full 255
  //then that 
}
